/**
 * A function that takes a string and number and returns void
 * @typedef {function(string, number)}
 */
var VoidFunction;

/**
 * A function that takes a string and returns a number
 * @typedef {function(string): number}
 */
var FooFunction;

/**
 * A function that takes an optional string parameter and returns a string
 * @typedef {function(string=): string}
 */
var OptionalStringParamFunc;

/**
 * A function that takes an optional string parameter and returns a number
 * @typedef {function(?string): string}
 */
var OptionalStringParamFunc2;

/**
 * A function that returns an object
 * @typedef {function(): *}
 */
var AnyFunc;